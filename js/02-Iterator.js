'use strict';

// Iterator

for (let i = 0; i<3; i++) {
    console.log(i);
}

// ASI
// 1. Adds a ; when two lines are seperated by an enter key
// 2. When two statements are separated by a }
// 3. Line terminator followed by a BREAK, CONTINUE, THROW or RETURN